//search all of application - search wines(name, region, type), search tags, search users

import React from "react";

const SearchBar = () => {
  return <div id="searchBar"></div>;
};

export default SearchBar;
